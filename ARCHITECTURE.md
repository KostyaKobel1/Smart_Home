# Smart Home - Архітектура Проєкту

## 📁 Структура Проєкту

```
Smart_Home/
├── css/                          # Стилі (модульна архітектура)
│   ├── styles.css               # Головний файл з імпортами
│   ├── base.css                 # Базові стилі, змінні, reset
│   ├── component-action.css     # Форми, кнопки, результати
│   ├── component-accordion.css  # Розгортаний список компонентів
│   ├── stats-dashboard.css      # Панель статистики
│   ├── event-log.css            # Журнал подій
│   ├── toast.css                # Спливаючі повідомлення
│   ├── animations.css           # Keyframe анімації
│   ├── responsive.css           # Медіа-запити
│   ├── global.header-nav.css    # Стилі шапки
│   ├── global.footer.css        # Стилі футера
│   └── header-background.css    # Фонові стилі для header
│
├── js/                          # JavaScript модулі
│   ├── index.js                 # Точка входу
│   ├── header-nav.js            # Логіка навігації
│   ├── index.components-manager.js  # UI менеджер компонентів
│   │
│   ├── models/                  # Моделі даних (ООП)
│   │   └── component.model.js   # Класи: Component, Light, Thermostat,
│   │                            #        SmartLock, Camera, Television
│   │
│   ├── services/                # Бізнес-логіка
│   │   └── smart-home-service.js  # SmartHomeService (CRUD, persistence)
│   │
│   └── handlers/                # Обробники подій
│       ├── smart-home-handlers.js  # Handlers для UI -> Service
│       └── component-select.js     # Робота з dropdown списками
│
├── img/                         # Зображення
│   └── header/
│       └── index-background-smart-home.jfif
│
├── index.html                   # Головна сторінка
├── global.header-nav.partial.html    # Partial: шапка (з inline SVG логотипом)
├── global.footer.partial.html        # Partial: футер
├── index.components-manager.partial.html  # Partial: UI компонентів
├── .gitignore                   # Git ignore файл
└── LICENSE
```

## 🏗️ Архітектурні Шари

### 1. **Models (Моделі)** - `js/models/`

Об'єктно-орієнтовані класи, що представляють компоненти розумного будинку.

**Базовий клас:** `Component`

- Властивості: `id`, `name`, `type`, `status`, `room`, `createdAt`, `lastUpdated`
- Методи: `turnOn()`, `turnOff()`, `getInfo()`, `getActions()`

**Підкласи з розширеними можливостями:**

- **`Light`** - керування яскравістю (0-100%)
- **`Thermostat`** - керування температурою (16-30°C)
- **`SmartLock`** - блокування/розблокування
- **`Camera`** - запис відео
- **`Television`** ⭐ (розширений компонент):
  - Гучність (0-100), mute/unmute
  - 10 каналів (перемикання +/-, прямий вибір)
  - Джерела сигналу: TV, HDMI1, HDMI2, USB
  - Список каналів

### 2. **Services (Сервіси)** - `js/services/`

Бізнес-логіка та управління станом.

**`SmartHomeService`:**

- **CRUD операції:** створення (з room), видалення, отримання списку компонентів
- **Виконання дій:** executeAction() з підтримкою всіх команд
- **Персистентність:** localStorage (auto-save/load)
- **Статистика:** total, online/offline, розподіл по типам (byType), розподіл по кімнатах (byRoom)
- **Event Log:** журнал останніх 50 подій з автооновленням при видимості
- **Reset modes:**
  - `event-log` - очищення лише журналу подій
  - `factory` - повне скидання (компоненти + журнал)

### 3. **Handlers (Обробники)** - `js/handlers/`

Тонкий шар між UI та Service.

**`smart-home-handlers.js`:**

- `handleCreateComponent(name, type, room, callbacks)` - створення компонента з кімнатою
- `handleDeleteComponent()` - видалення
- `handleComponentAction()` - виконання дій
- `handleGetStats()` - статистика (включно з byRoom)
- `handleGetComponents()` - отримання списку компонентів
- `handleGetEventLog()` - журнал подій
- `handleReset(mode)` - скидання системи з вибором режиму
- **Валідація типів:** `isTypeAllowedForName()` - перевірка відповідності назви та типу
- **Автовизначення:** `detectTypeFromName()` - визначення типу за ключовими словами (включно з lamp/light)

**`component-select.js`:**

- `updateSelectOptions()` - оновлення випадаючого списку компонентів (з відображенням room)
- `getSelectedComponent()` - отримання обраного компонента
- `clearSelectValue()` - очищення вибору
- Labels включають room: `"name (type • room)"`

### 4. **UI Manager** - `js/index.components-manager.js`

Керування DOM та користувацьким інтерфейсом.

**Основні функції:**

- Рендеринг акордеону з компонентами (з відображенням кімнати)
- Динамічна генерація елементів управління на основі `getActions()`
- Групування дій (Power, Lock, Recording, Volume, Channel, Input)
- Toast-повідомлення про успіх/помилку (з іконками: ✓, ✕, ℹ, ✨, 🗑️, 🔄)
- Оновлення статусу в реальному часі
- **Автооновлення панелей:**
  - Статистика (auto-refresh) при відкритій панелі
  - Список компонентів при створенні/видаленні (якщо список відкритий)
  - Event Log при створенні/видаленні/діях (якщо log відкритий)
- **Room-based функціонал:**
  - Фільтр по кімнатах для списку компонентів
  - Двоступеневий вибір для видалення (кімната → компонент)
  - Поле Room у формі/модальному вікні створення з datalist підказками
  - Статистика з перемикачем Global/Filtered режимів
- **Модальні вікна (єдиний патерн):**

  - Create Component: модальне вікно з полями Name, Type, Room; попереднє заповнення Room з активного фільтра; валідація назви; кнопки Cancel/Create; підтримка Enter (submit) та Esc (cancel).
  - Remove Component: модальне вікно з каскадним вибором Room → Component; підтвердження видалення; кнопки Cancel/Remove.
  - Reset Data: модальне вікно з режимами `event-log` та `factory`; допоміжні описи; окремі підтвердження.

- **Стійке прив’язування подій:**

  - Прив’язка через `bindOnce` (уникає дублювання слухачів при повторних ініціалізаціях).
  - Автоматичне перев’язування після `htmx:afterSwap` (коли partial секції замінюються).
  - Ініціалізація не падає, навіть якщо деяких необов’язкових елементів немає — прив’язує те, що доступне.

- Кешування DOM елементів для оптимізації продуктивності

- Примітка: стара inline-форма створення збережена у DOM (прихована) для зворотної сумісності, однак UX реалізовано через модальне вікно.

### 5. **CSS Modules** - `css/`

Модульна структура стилів для кращої читабельності.

**Розділення за функціональністю:**

- **base.css** - змінні, reset, базова типографіка
- **component-action.css** - форми, кнопки, стилі кнопок за типами, фільтр кімнат з відступом та border-top; спільні стилі модальних вікон (`.reset-dialog-overlay`, `.reset-dialog`, `.reset-dialog__select`, `.reset-dialog__buttons`) використовуються для Reset, Remove та Create діалогів.
- **component-accordion.css** - розгортаний список з анімацією
- **stats-dashboard.css** - градієнт, сітка статистики, стилізовані кнопки перемикання режимів (Global/Filtered)
- **event-log.css** - стилі журналу подій, scrollbar
- **toast.css** - спливаючі сповіщення
- **animations.css** - @keyframes (fadeIn, slideIn, fadeOut)
- **responsive.css** - @media queries для мобільних

**Примітка:** Всі CSS модулі використовують вкладений SCSS-подібний синтаксис для кращої структури та читабельності.

## 🔄 Потік Даних

```
┌─────────────────┐
│   User Action   │
│   (Click btn)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  UI Manager     │◄──────┐
│ (components-    │       │
│  manager.js)    │       │
└────────┬────────┘       │
         │                │
         ▼                │
┌─────────────────┐       │
│   Handlers      │       │
│ (smart-home-    │       │
│  handlers.js)   │       │
└────────┬────────┘       │
         │                │
         ▼                │
┌─────────────────┐       │
│   Service       │       │
│ (smart-home-    │       │
│  service.js)    │       │
└────────┬────────┘       │
         │                │
         ▼                │
┌─────────────────┐       │
│    Models       │       │
│ (component.     │       │
│  model.js)      │       │
└────────┬────────┘       │
         │                │
         ▼                │
┌─────────────────┐       │
│  localStorage   │       │
│   (persist)     │       │
└─────────────────┘       │
                          │
    ┌─────────────────────┘
    │ Update UI
    │ (toast, status)
```

## 🎯 Ключові Принципи ООП

### 1. **Інкапсуляція**

- Приватні дані в класах (`#createFromData`)
- Публічні методи для взаємодії (`getInfo()`, `getActions()`)

### 2. **Наслідування**

```javascript
Component (базовий)
  ├── Light extends Component
  ├── Thermostat extends Component
  ├── SmartLock extends Component
  ├── Camera extends Component
  └── Television extends Component (розширений)
```

### 3. **Поліморфізм**

- `getActions()` - кожен підклас повертає свій набір дій
- `getInfo()` - перевизначається для додавання специфічних властивостей

### 4. **Абстракція**

- Service приховує деталі роботи з localStorage
- Handlers абстрагують UI від бізнес-логіки

## 📊 Функціональність

### ✅ Виконані вимоги:

1. **5 типів компонентів** (Light, Thermostat, SmartLock, Camera, Television)
2. **Web-інтерфейс** з роботою через DOM API
3. **Конфігурація:** додавання/видалення з вибором типу та кімнати
4. **Управління:** on/off/toggle + специфічні команди для кожного типу
5. **Додатковий функціонал:**
   - **Підтримка кімнат (Rooms):**
     - Створення компонентів з прив'язкою до кімнати
     - Фільтрація списку компонентів по кімнатах
     - Двоступеневий вибір при видаленні (кімната → компонент)
     - Статистика по кімнатах (byRoom)
     - Перемикач режимів статистики: Global ↔ Filtered by Room
   - **Статистика** (total, online/offline, byType, byRoom) з auto-refresh
   - **Event Log** (журнал останніх 50 подій) з автооновленням
   - **Reset Data з модальним вікном:**
     - Режим `event-log`: очищення лише журналу подій
     - Режим `factory`: повне скидання всіх даних
     - Select замість prompt для вибору режиму
     - Описи та окремі підтвердження для кожного режиму
   - Валідація створення компонентів (тип повинен відповідати назві)
   - Автоматичне визначення типу за ключовими словами в назві (включно lamp/light)
   - Toast-повідомлення з різними іконками для різних типів подій
   - Автоматичне оновлення UI при змінах (якщо панелі відкриті)
6. **ООП:** повна підтримка наслідування, інкапсуляції, поліморфізму
7. **Розширений компонент (Television):**
   - 13 різних команд управління
   - Гучність з mute/unmute
   - 10 каналів з різними режимами перемикання
   - 4 джерела входу (TV, HDMI1/2, USB)

## 🚀 Технології

- **Vanilla JavaScript** (ES6+ modules)
- **CSS3** (CSS Variables, Flexbox, Grid, Animations)
- **HTML5** (Semantic markup)
- **HTMX** (для динамічного завантаження partials, з повторним прив’язуванням UI-подій після `htmx:afterSwap`)
- **localStorage** (персистентність даних)

## 📝 Використання

1. Відкрити `index.html` у браузері
2. Натиснути "Create Component" та заповнити модальне вікно:

- Ввести назву компонента (наприклад, "Ceiling Light", "Front Door Lock")
- Вибрати тип компонента зі списку
- (Опціонально) Вказати кімнату або вибрати з підказок (Living Room, Kitchen, Bedroom...)
- Підтвердити кнопкою Create або клавішею Enter (Esc — скасувати)

3. Натиснути "Get List Of Components" для перегляду
4. Використати фільтр "Filter by Room" для фільтрації компонентів по кімнатах
5. Розгорнути панель компонента для керування діями
6. У статистиці використати кнопку "Show Room Only" / "Show Global" для перемикання режимів відображення
7. Натиснути "Get List Of Components" для перегляду
8. Використати фільтр "Filter by Room" для фільтрації компонентів по кімнатах
9. Розгорнути панель компонента для керування діями
10. У статистиці використати кнопку "Show Room Only" / "Show Global" для перемикання режимів відображення

## 🛠️ Troubleshooting

- Модальне вікно не відкривається:

  - Переконайтесь, що події перев’язані після завантаження partials (слухач `htmx:afterSwap` викликає повторне `cacheElements()` і `bindEvents()`).
  - Кнопка "Create Component" має id `create-component-btn`; перевірте наявність елемента в DOM.
  - Спробуйте оновити сторінку, якщо partial був замінений без повторної ініціалізації.

- Подвійні спрацювання кліку:

  - Використовується `bindOnce` для уникнення дублюючих слухачів; перевірте, що слухачі не прив’язуються вручну в інших місцях.

- Помилка валідації типу при створенні:

  - Тип повинен відповідати назві (напр., для `Light` в назві бажано містити "light"/"lamp"). Повідомлення: "You have selected the wrong type for the component".
  - Альтернатива — обрати тип `generic` (дозволено для довільних назв) або скоригувати назву.

- Клавіатурні скорочення в модалках не працюють:

  - Enter = підтвердити; Esc = скасувати. Переконайтесь, що фокус всередині модального вікна.

- Не оновлюється список/статистика після створення/видалення:
  - Перевірте, чи панелі відкриті (автооновлення працює лише коли вони відображені). В коді викликаються `autoRefreshStats()`, `autoRefreshComponentsList()`, `autoRefreshEventLog()` після успішних операцій.

## 🖼️ Media (Screenshots/GIFs)

- Create Component (modal): [PLACEHOLDER — insert screenshot]
- Remove Component (modal): [PLACEHOLDER — insert screenshot]
- Reset Data (modal): [PLACEHOLDER — insert screenshot]
- Components List (accordion): [PLACEHOLDER — insert screenshot]
- Statistics Dashboard: [PLACEHOLDER — insert screenshot]

## 🎨 UI/UX Features

- **Акордеон** - плавне розгортання панелей управління
- **Toast-повідомлення** - feedback про успішність операцій з іконками
- **Динамічні елементи** - кнопки/слайдери генеруються на основі моделі
- **Inline-оновлення** - статус змінюється без перезавантаження списку
- **Responsive** - адаптивний дизайн для мобільних пристроїв
- **Анімації** - fadeIn, slideIn для плавної появи елементів
- **Inline SVG** - логотип та favicon використовують inline SVG (data URI)
- **Auto-refresh:**
  - Статистика автоматично оновлюється при відкритій панелі
  - Список компонентів автоматично оновлюється при створенні/видаленні
  - Event Log автоматично оновлюється при створенні/видаленні/діях
- **Room-based UI:**
  - Фільтр по кімнатах з dropdown меню (з іконкою 🏠, відступом та border-top)
  - Відображення кімнати у списку компонентів та в meta accordion
  - Двоступеневий select для видалення (кімната → компонент)
  - Datalist з підказками популярних кімнат при створенні
  - Синхронізація фільтра з полем створення (при зміні фільтра оновлюється поле)
  - Автоматичне оновлення списку компонентів при зміні фільтра
- **Статистика з режимами:**
  - Global режим: загальна статистика всієї системи з розподілом по кімнатах
  - Filtered режим: статистика лише для обраної кімнати (total, online/offline, byType)
  - Перемикач "Show Room Only" / "Show Global" (стилізована кнопка з border, backdrop-blur, hover ефектами)
  - Динамічне оновлення при зміні режиму або фільтра
  - Заголовок змінюється відповідно до режиму ("System Statistics" / "Statistics — Room Name")
- **Reset Data модальне вікно:**
  - Стилізований select замість prompt
  - Описи для кожного режиму
  - Кнопки Cancel/Reset з gradient та hover ефектами
  - Backdrop blur для overlay
